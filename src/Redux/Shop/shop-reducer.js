import ShopData from './ShopData';

const INITIAL_SATE ={
    ShopData
}

export const shpoReducer = (state = INITIAL_SATE, action) =>{
    switch(action.type){

        default:
            return state
    }
}